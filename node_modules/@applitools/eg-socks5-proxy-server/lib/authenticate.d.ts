import { AuthenticatePatameters } from '../types/authenticate';
/**
 * +----+------+----------+------+----------+
 * |VER | ULEN |  UNAME   | PLEN |  PASSWD  |
 * +----+------+----------+------+----------+
 * | 1  |  1   | 1 to 255 |  1   | 1 to 255 |
 * +----+------+----------+------+----------+
 *
 *
 * @param {Buffer} buffer - a buffer
 * @returns {undefined}
 **/
export declare function authenticate({ socket, buffer, options, connect, end, onAuthenticated, onFailed, }: AuthenticatePatameters): void;
//# sourceMappingURL=authenticate.d.ts.map