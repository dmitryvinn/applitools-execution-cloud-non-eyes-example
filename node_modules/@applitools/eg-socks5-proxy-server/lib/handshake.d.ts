import { HandshakePatameters } from '../types/handshake';
/**
 * +----+----------+----------+
 * |VER | NMETHODS | METHODS  |
 * +----+----------+----------+
 * | 1  |    1     | 1 to 255 |
 * +----+----------+----------+
 *
 * @param {Socket} socket - a socket
 * @param {Buffer} buffer - a buffer
 * @param {any} options
 * @param {Function} connet
 * @param {Function} authenticate
 * @param {Function} end
 * @param {Function} onHandshakeCompleted
 *
 * @returns {undefined}
 **/
export declare function handshake({ socket, buffer, options, connect, authenticate, end, onHandshakeCompleted, }: HandshakePatameters): void;
//# sourceMappingURL=handshake.d.ts.map