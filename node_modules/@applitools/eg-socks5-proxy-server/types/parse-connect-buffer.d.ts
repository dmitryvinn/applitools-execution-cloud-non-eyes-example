import {Buffer} from 'buffer'

export interface ConnectBufferObject {
  ver: number
  cmd: number
  rsv: number
  atyp: number
  requestBuffer: Buffer
  dst: {addr: string; port: number}
  addr: {buf: Buffer}
}

export interface ParseConnectBufferParameters {
  buffer: Buffer
  onReqesutBufferParsed: () => void
  onParseCompleted: (args: ConnectBufferObject) => void
  end: (rpcReply: number, args: any) => void
}
