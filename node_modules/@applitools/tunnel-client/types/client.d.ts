import type { TunnelClient, TunnelClientSettings } from './types';
import { type Logger } from '@applitools/logger';
export declare function makeTunnelClient({ settings, logger: defaultLogger, }: {
    settings?: TunnelClientSettings;
    logger?: Logger;
}): TunnelClient;
