import type { RenderTarget, RenderSettings, RenderResult } from './types';
import { type AbortSignal } from 'abort-controller';
import { type UFGRequests } from './server/requests';
import { type Logger } from '@applitools/logger';
type Options = {
    requests: UFGRequests;
    concurrency?: number;
    timeout?: number;
    batchingTimeout?: number;
    pollingTimeout?: number;
    logger: Logger;
};
export declare function makeRender({ requests, concurrency, timeout, batchingTimeout, pollingTimeout, logger: mainLogger, }: Options): ({ target, settings, signal, logger, }: {
    target: RenderTarget;
    settings: RenderSettings;
    signal?: AbortSignal | undefined;
    logger?: Logger | undefined;
}) => Promise<RenderResult>;
export {};
