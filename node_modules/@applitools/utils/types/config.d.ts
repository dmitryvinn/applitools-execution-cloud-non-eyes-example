export declare function getConfig({ baseDir, paths, params, traverse, strict, logger, }?: {
    baseDir?: string;
    paths?: string[];
    params?: string[];
    traverse?: false | {
        stopDir: string;
    };
    strict?: boolean;
    logger?: any;
}): Record<string, any>;
