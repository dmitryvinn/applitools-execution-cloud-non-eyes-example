import type { ECClient, ECClientSettings } from './types';
import { type Logger } from '@applitools/logger';
export declare function makeECClient({ settings, logger: defaultLogger, }?: {
    settings?: Partial<ECClientSettings>;
    logger?: Logger;
}): Promise<ECClient>;
