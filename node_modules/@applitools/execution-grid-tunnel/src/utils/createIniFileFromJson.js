const {promises: fs} = require('fs')
const ini = require('ini')

module.exports = async function ({path, content}) {
  const iniString = ini.stringify(content)
  await fs.writeFile(path, iniString)
}
